import { api_key } from "./util.js";
import randomString from "./util.js"; // default import can be named anything.
import { abc, def } from "./util.js";

console.log(api_key);
console.log(randomString);
console.log(abc);
console.log(def);